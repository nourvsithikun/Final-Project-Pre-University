# Background artwork

`angkor_night.png` is the included environment-only Angkor night artwork. It deliberately contains no ships, words, HUD values, lasers, or statistics; those remain dynamic C++ layers. The game draws a procedural fallback if this image is removed.
